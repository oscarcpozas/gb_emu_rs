[package]
name = "gb-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
console_error_panic_hook = "0.1"
gb-emu = { package = "gb-emu", path = "../gb-emu" }
wasm-bindgen = "0.2"
